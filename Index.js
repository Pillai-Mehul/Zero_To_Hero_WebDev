// --- Initial Declaration ---
// Stores a specific string value in a variable
var name = "Mehul";

// Triggers a browser alert to display the stored name
alert("My Name is " + name);

// --- User Interaction ---
// Opens an input dialog to capture the user's name
var yourName = prompt("What is your Name?");

// Combines a greeting with the user-provided input and displays it
alert("Nice to Meet You " + yourName);
